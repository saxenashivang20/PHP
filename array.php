<?php
// Index array
//$cars =array("kia","hyundai","tata");
// $mnc = array("apple","facebook","google");

//associative array
//  $arr=["hello"=>2,3,"hola"=>4,5,6,"bonjour"=>7];

// print_r (array_change_key_case($arr,1)); 
// to uppercase.

//print_r(array_chunk($data,3)); 
// to divide the array into 3 parts.

// print_r(array_combine($arr,$ar)); 
// it combines two array ,first array uses as key and second array as value, both array should have same number of elements.

// print_r(array_column());

// print_r(array_count_values($data)); 
// to check the frequency of array. how many times a value is repeating.

// print_r(array_diff($arr,$rem)); 
// returns in the form of associative array and outputs the different element in second array.

// print_r(array_diff_assoc(["garry"=>10,"harry"=>20],["harry"=>20]));

// $arr =array_fill(2,4,6); 
// (no. of index start from, length of array, value to be staored in given array.)

// print_r($arr);

// print_r(array_intersect([10,20,30,20],[40,30]));
//  to find the common in 2 different array.

// print_r(array_intersect_key([10,20,30,20],[40,30]));
// print_r(array_key_exists("apple",$mnc));
//  it returns true if key exists (only work for associative).

// print_r(array_keys($arr)); 
// print_r(array_sum($arr)); 
// it returns sum of all keys.
// (both index and associative)

$data=[50,30,35,26,8];
$new =["y"=>80,"m"=>90,"o"=>85];
// print_r(array_push($data)); 
// add element in the last index of array.

// print_r($data);

// unset($data[2]);
//  delete the value of index number which are given to function. 

// print_r(array_pop($arr)); 
// delete the last element in array.

// print_r(array_reverse()); 
// it reverse the array. 

// print_r(array_replace($data));

// print_r(array_merge($data,$arr)); 
// it merge 2 different array and provide 1 as output.

// print_r(array_search(10,$data));
// search the element and returns it.

// print_r(array_splice($data,1,2,$arr));
// it used to insert elements of 2nd array into 1st array acco to index.
// print_r($data);

// print_r(array_slice($data,1));
// it start printing from the provided index and delete the previous elements.
// sort functions 
sort()
ksort()


//range(1,100);
// it will return value from 1 to 100





?>